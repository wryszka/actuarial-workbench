# Build artifacts
src/app/frontend/dist/
src/app/frontend/node_modules/
node_modules/

# Python
.venv/
__pycache__/
*.pyc

# Databricks bundle state
.databricks/

# Sensitive GTM data — real account names, consumption, deal stages.
# The parser/seed scripts are versioned; the data they read/emit is NOT.
# To rebuild: export the UKI sheet/plan to data/ then `make parse && make seed`.
data/

# Impact seed names real customer accounts + engagement/C-level detail — keep
# local only (evidence lives in the promo deck / evidence pack / kudos, not here).
scripts/impact_data.py

*.bak
